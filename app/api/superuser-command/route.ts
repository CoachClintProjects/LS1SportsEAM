import { NextRequest, NextResponse } from 'next/server';
import { requireSuperUser, SuperUserAuthError } from '@/lib/server/requireSuperUser';
import { writeAuditEvent } from '@/lib/server/writeAuditEvent';
const URL=process.env.NEXT_PUBLIC_SUPABASE_URL,KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;export const dynamic='force-dynamic';export const revalidate=0;const headers=()=>({apikey:KEY!,Authorization:`Bearer ${KEY!}`,'Content-Type':'application/json',Prefer:'return=representation'});async function rest(path:string,init:RequestInit={}){if(!URL||!KEY)throw new Error('SuperUser server credentials are not configured.');const r=await fetch(`${URL}/rest/v1/${path}`,{...init,headers:{...headers(),...(init.headers||{})},cache:'no-store'}),t=await r.text();if(!r.ok)throw new Error(`Supabase ${path} returned ${r.status}: ${t.slice(0,500)}`);return t?JSON.parse(t):null}function authError(e:unknown){return e instanceof SuperUserAuthError?NextResponse.json({error:e.message},{status:e.status}):null}
type Evidence=Record<string,unknown>;type Unit={status?:string;evidence_ref?:string|null;verified_at?:string|null;evidence?:Evidence|null;implementation_percent?:number|string|null;operational_percent?:number|string|null;validation_percent?:number|string|null;[key:string]:unknown};const truth=(e:Evidence,k:string[])=>k.some(x=>e[x]===true),pct=(v:unknown)=>{const n=Number(v);return Number.isFinite(n)?Math.max(0,Math.min(100,n)):0};
function deriveUnitMeasurement(unit:Unit){const evidence=unit.evidence&&typeof unit.evidence==='object'?unit.evidence:{},status=String(unit.status||'').toLowerCase(),trace=Boolean(unit.evidence_ref)&&!['planned','not_started','placeholder'].includes(status),implementationCertified=trace&&(truth(evidence,['implementation_certified','build_green','regression_lock'])||['implemented','implemented_unverified','observed','operational','verified','complete'].includes(status)),operationalCertified=implementationCertified&&truth(evidence,['operational_certified','runtime_certified','browser_certified','live_workflow_certified','production_certified']),validationCertified=operationalCertified&&Boolean(unit.verified_at)&&truth(evidence,['validation_certified','e2e_certified','acceptance_certified','regression_certified']);return{...unit,implementation_percent:pct(unit.implementation_percent),operational_percent:pct(unit.operational_percent),validation_percent:pct(unit.validation_percent),measurement:{mode:'evidence_progress_v2',implementationCertified,operationalCertified,validationCertified,readinessCertified:implementationCertified&&operationalCertified&&validationCertified,traceableArtifact:trace,measuredAt:new Date().toISOString(),note:'Progress percentages remain the canonical tracked readiness measurements. Certification gates are reported separately and control final 100% readiness; missing certification no longer erases measured operational progress to zero.'}}}
export async function GET(request:NextRequest){try{const actor=await requireSuperUser(request);if(!URL||!KEY)throw new Error('SuperUser server credentials are not configured.');const r=await fetch(`${URL}/rest/v1/rpc/get_superuser_command`,{method:'POST',headers:{...headers(),Accept:'application/json'},body:'{}',cache:'no-store'}),body=await r.text();if(!r.ok)throw new Error(`Supabase SuperUser RPC returned ${r.status}: ${body.slice(0,400)}`);const payload=JSON.parse(body),units=Array.isArray(payload?.units)?payload.units.map(deriveUnitMeasurement):[];return NextResponse.json({...payload,units,measurement:{mode:'evidence_progress_v2',manualPercentagesUsedInRollup:true,readinessRule:'tracked progress is shown continuously; final readiness still requires implementation, operational and validation certification'},operator:{id:actor.operatorId,email:actor.email,displayName:actor.displayName,canOnboardClients:actor.canOnboardClients,canManagePlatformSettings:actor.canManagePlatformSettings}},{headers:{'Cache-Control':'no-store, max-age=0'}})}catch(e){const a=authError(e);if(a)return a;return NextResponse.json({project:null,milestones:[],tasks:[],raci:[],units:[],inventory:[],counts:{},generatedAt:new Date().toISOString(),source:'LS1SportsEAM Supabase',error:e instanceof Error?e.message:'Command data unavailable'},{status:500})}}
export async function POST(request:NextRequest){try{const actor=await requireSuperUser(request),body=await request.json(),action=String(body.action||'');if(action==='update-task'){if(!actor.canManagePlatformSettings)throw new SuperUserAuthError('Platform-management permission required.',403);const before=await rest(`platform_project_tasks?id=eq.${encodeURIComponent(body.id)}&select=*&limit=1`),row=await rest(`platform_project_tasks?id=eq.${encodeURIComponent(body.id)}`,{method:'PATCH',body:JSON.stringify({status:body.status,percent_complete:Number(body.percent_complete),blocker:body.blocker||null,updated_at:new Date().toISOString()})});await writeAuditEvent(actor,{action:'SUPERUSER_TASK_UPDATED',entityType:'platform_project_tasks',entityId:body.id,beforeData:before?.[0]||null,afterData:row?.[0]||body,reason:'Super User project-control update'});return NextResponse.json(row?.[0]||null)}if(action==='update-unit'){if(!actor.canManagePlatformSettings)throw new SuperUserAuthError('Platform-management permission required.',403);const before=await rest(`platform_milestone_units?id=eq.${encodeURIComponent(body.id)}&select=*&limit=1`),row=await rest(`platform_milestone_units?id=eq.${encodeURIComponent(body.id)}`,{method:'PATCH',body:JSON.stringify({status:body.status,implementation_percent:Number(body.implementation_percent??before?.[0]?.implementation_percent??0),operational_percent:Number(body.operational_percent??before?.[0]?.operational_percent??0),validation_percent:Number(body.validation_percent??before?.[0]?.validation_percent??0),evidence:body.evidence||{},evidence_source:'SuperUser evidence update',evidence_ref:body.evidence_ref||null,verified_at:body.verified?new Date().toISOString():before?.[0]?.verified_at||null})});await writeAuditEvent(actor,{action:'SUPERUSER_UNIT_EVIDENCE_UPDATED',entityType:'platform_milestone_units',entityId:body.id,beforeData:before?.[0]||null,afterData:row?.[0]||body,reason:'SuperUser evidence and tracked-readiness update'});return NextResponse.json(row?.[0]?deriveUnitMeasurement(row[0]):null)}if(action==='create-ticket'){const tenant=(await rest('tenants?select=id&limit=1'))?.[0]?.id||null,max=await rest('support_tickets?select=ticket_number&order=ticket_number.desc&limit=1'),ticketNumber=Number(max?.[0]?.ticket_number||0)+1,row=await rest('support_tickets',{method:'POST',body:JSON.stringify({tenant_id:tenant,ticket_number:ticketNumber,title:body.title,description:body.description||null,category:body.category||'PRODUCT',severity:body.severity||'MEDIUM',status:'OPEN'})});await writeAuditEvent(actor,{action:'SUPPORT_TICKET_CREATED',entityType:'support_tickets',entityId:row?.[0]?.id||null,tenantId:tenant,afterData:row?.[0]||body,reason:'Super User support ticket creation'});return NextResponse.json(row?.[0]||null)}if(action==='create-client'){if(!actor.canOnboardClients)throw new SuperUserAuthError('Client-onboarding permission required.',403);const tenant=(await rest('tenants?select=id&limit=1'))?.[0]?.id||null,row=await rest('client_onboarding_cases',{method:'POST',body:JSON.stringify({client_name:body.client_name,status:'DISCOVERY',tenant_id:tenant,primary_admin_email:body.primary_admin_email||null,sports:body.sports?.length?body.sports:['SWIMMING'],current_step:1,notes:{created_from:'superuser',operator_id:actor.operatorId}})});await writeAuditEvent(actor,{action:'CLIENT_ONBOARDING_CREATED',entityType:'client_onboarding_cases',entityId:row?.[0]?.id||null,tenantId:tenant,afterData:row?.[0]||body,reason:'Super User client onboarding creation'});return NextResponse.json(row?.[0]||null)}if(action==='register-result-import'){if(!actor.canManagePlatformSettings)throw new SuperUserAuthError('Platform-management permission required.',403);const tenant=(await rest('tenants?select=id&limit=1'))?.[0]?.id||null,systems=await rest('competition_source_systems?select=id,name,code&limit=100'),source=systems?.find((i:{name?:string;code?:string})=>{const n=String(body.source_system||'').toLowerCase();return String(i.name||'').toLowerCase()===n||String(i.code||'').toLowerCase()===n})?.id||null,job=await rest('import_jobs',{method:'POST',body:JSON.stringify({tenant_id:tenant,source_system:body.source_system||null,source_file:body.original_filename||null,entity_type:'COMPETITION_RESULTS',started_at:new Date().toISOString(),status:'RECEIVED',records_read:0,records_valid:0,records_rejected:0,records_created:0,records_updated:0})}),row=await rest('competition_import_files',{method:'POST',body:JSON.stringify({tenant_id:tenant,import_job_id:job?.[0]?.id||null,source_system_id:source,original_filename:body.original_filename,detected_format:body.detected_format||null,file_size_bytes:body.file_size_bytes||null,parse_status:'queued',parse_summary:{registered_by:actor.email,note:body.note||null}})});await writeAuditEvent(actor,{action:'COMPETITION_IMPORT_REGISTERED',entityType:'competition_import_files',entityId:row?.[0]?.id||null,tenantId:tenant,afterData:row?.[0]||body,reason:'Super User competition source registration'});return NextResponse.json(row?.[0]||null)}return NextResponse.json({error:'Unsupported SuperUser action.'},{status:400})}catch(e){const a=authError(e);if(a)return a;return NextResponse.json({error:e instanceof Error?e.message:'Action failed'},{status:400})}}
