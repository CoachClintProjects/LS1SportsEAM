'use client'; import{useEffect,useState}from'react';import{authenticatedFetch}from'@/lib/client/authenticatedFetch';
export function Reporting(){const[data,setData]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{authenticatedFetch('/api/admin-command',{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j)}).catch(e=>setError(e.message))},[]);if(!data)return <State text={error||'Loading canonical reporting data…'}/>;const rows=[['Open receivables',data.metrics.openInvoices],['Past-due receivables',data.metrics.pastDue],['AR balance',data.metrics.arBalance],['AP balance',data.metrics.apBalance],['Active athletes',data.metrics.activeAthletes],['Active teams',data.metrics.activeTeams],['Open admin work',data.tasks.length]];return <main className="p-5 text-white lg:p-7"><div className="text-[9px] font-black uppercase tracking-[.22em] text-[#FA4616]">Organization Administrator · Reporting</div><h1 className="mt-1 text-3xl font-black">Canonical reporting</h1><p className="mt-2 text-sm text-neutral-400">Current roll-up from authorized LS1 records. Reporting does not grant additional authority.</p><section className="mt-6 overflow-hidden rounded-2xl border border-neutral-800 bg-[#090b0b]"><table className="w-full text-sm"><thead className="text-left text-neutral-500"><tr><th className="p-4">Measure</th><th>Current canonical value</th><th>Source</th></tr></thead><tbody>{rows.map(([n,v])=><tr key={n} className="border-t border-neutral-800"><td className="p-4 font-bold">{n}</td><td>{String(v)}</td><td className="text-neutral-500">LS1SportsEAM canonical store</td></tr>)}</tbody></table></section><p className="mt-4 text-xs text-neutral-600">Generated {new Date(data.generatedAt).toLocaleString()}. No synthetic reports, trend percentages, or processing states are generated.</p></main>}function State({text}:{text:string}){return <div className="p-8 text-white"><h1 className="text-2xl font-black">Reporting</h1><p className="mt-2 text-neutral-500">{text}</p></div>}export default Reporting;