'use client';import{useEffect,useState}from'react';import{authenticatedFetch}from'@/lib/client/authenticatedFetch';export function AdminCompetitionOperations(){const[data,setData]=useState<any[]>([]),[error,setError]=useState(''),[selected,setSelected]=useState<any>(null);useEffect(()=>{authenticatedFetch('/api/admin-command',{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.controls?.competitions||[])}).catch(e=>setError(e.message))},[]);return <main className="p-5 text-white lg:p-7"><div className="text-[9px] font-black uppercase tracking-[.22em] text-[#FA4616]">Organization Administrator · Live records</div><h1 className="mt-1 text-3xl font-black">Admin Competition Operations</h1>{error&&<p className="mt-4 text-red-300">{error}</p>}<section className="mt-5 overflow-hidden rounded-2xl border border-neutral-800 bg-[#090b0b]"><div className="divide-y divide-neutral-800">{data.map((r:any)=><button key={r.id} onClick={()=>setSelected(r)} className="grid w-full grid-cols-[1fr_auto] gap-4 p-4 text-left hover:bg-white/[.03]"><span className="font-bold">{r.name||r.bill_number||r.id}</span><span className="text-xs uppercase text-neutral-500">{r.status||r.competition_type||''}</span></button>)}</div>{!data.length&&!error&&<div className="p-8 text-sm text-neutral-500">No canonical competition records exist in the authorized organization scope.</div>}</section>{selected&&<div className="fixed inset-0 z-[100] bg-black/70" onClick={()=>setSelected(null)}><aside onClick={e=>e.stopPropagation()} className="ml-auto h-full w-full max-w-2xl overflow-y-auto border-l border-neutral-700 bg-[#080909] p-6"><button onClick={()=>setSelected(null)} className="float-right text-neutral-400">Close</button><div className="mt-8 grid gap-3">{Object.entries(selected).map(([k,v])=><div key={k} className="border-b border-neutral-800 pb-2"><div className="text-[9px] uppercase text-neutral-600">{k.replaceAll('_',' ')}</div><div className="break-words text-sm">{typeof v==='object'?JSON.stringify(v):String(v??'—')}</div></div>)}</div></aside></div>}</main>}export default AdminCompetitionOperations;