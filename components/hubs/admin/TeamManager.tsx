'use client';import{useEffect,useMemo,useState}from'react';import{authenticatedFetch}from'@/lib/client/authenticatedFetch';
export function TeamManager({role='team_manager'}:{role?:string}){const[data,setData]=useState<any>(null),[error,setError]=useState(''),[q,setQ]=useState(''),[selected,setSelected]=useState<any>(null);useEffect(()=>{authenticatedFetch(`/api/admin-command?role=${encodeURIComponent(role)}`,{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.registrar)}).catch(e=>setError(e.message))},[role]);const athletes=useMemo(()=>data?.athletes||[],[data]);const memberships=useMemo(()=>data?.teamMemberships||[],[data]);const rows=useMemo(()=>athletes.filter((a:any)=>JSON.stringify(a).toLowerCase().includes(q.toLowerCase())),[athletes,q]);if(!data)return <State text={error||'Loading authorized roster records…'}/>;return <main className="p-5 text-white lg:p-7"><div className="text-[9px] font-black uppercase tracking-[.22em] text-[#FA4616]">{role.replaceAll('_',' ')} · Roster</div><h1 className="mt-1 text-3xl font-black">Team roster operations</h1><p className="mt-2 text-sm text-neutral-400">Canonical athlete and team-membership records in the authorized scope. Unsupported edit, email and add-athlete controls are not rendered.</p>{error&&<p className="mt-3 text-red-300">{error}</p>}<section className="mt-5 overflow-hidden rounded-2xl border border-neutral-800 bg-[#090b0b]"><div className="flex items-center justify-between gap-3 border-b border-neutral-800 p-4"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter athletes" className="w-full max-w-sm rounded-lg border border-neutral-700 bg-black px-3 py-2 text-sm"/><span className="text-xs text-neutral-500">{memberships.length} roster memberships</span></div><table className="w-full text-sm"><thead className="text-left text-neutral-500"><tr><th className="p-4">Athlete #</th><th>Status</th><th>Privacy</th><th>Team memberships</th></tr></thead><tbody>{rows.map((a:any)=>{const m=memberships.filter((x:any)=>x.athlete_id===a.id);return <tr key={a.id} onClick={()=>setSelected({...a,__memberships:m})} className="cursor-pointer border-t border-neutral-800 hover:bg-white/[.03]"><td className="p-4 font-bold">{a.athlete_number||a.id}</td><td>{a.status||a.athlete_status||'—'}</td><td>{a.privacy_level||'—'}</td><td>{m.length}</td></tr>})}</tbody></table>{!rows.length&&<div className="p-8 text-sm text-neutral-500">No canonical athletes exist in the authorized roster scope.</div>}</section>{selected&&<div className="fixed inset-0 z-[120] bg-black/70" onClick={()=>setSelected(null)}><aside onClick={e=>e.stopPropagation()} className="ml-auto h-full w-full max-w-2xl overflow-y-auto border-l border-neutral-700 bg-[#080909] p-6"><button onClick={()=>setSelected(null)}>Close</button><div className="mt-6 grid gap-3 sm:grid-cols-2">{Object.entries(selected).map(([k,v])=><div key={k} className="border-b border-neutral-800 pb-2"><div className="text-[9px] uppercase text-neutral-600">{k.replaceAll('_',' ')}</div><div className="break-words text-sm">{typeof v==='object'?JSON.stringify(v):String(v??'—')}</div></div>)}</div></aside></div>}</main>}function State({text}:{text:string}){return <div className="p-8 text-white"><h1 className="text-2xl font-black">Team roster operations</h1><p className="mt-2 text-neutral-500">{text}</p></div>}export default TeamManager;