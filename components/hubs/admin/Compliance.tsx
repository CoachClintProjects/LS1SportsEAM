'use client';import{useEffect,useMemo,useState}from'react';import{authenticatedFetch}from'@/lib/client/authenticatedFetch';
type Domain='dataQualityIssues'|'duplicateCandidates'|'credentials'|'backgroundChecks'|'safeSport'|'waivers';
export function Compliance({role='org_admin'}:{role?:string}){const[data,setData]=useState<any>(null),[error,setError]=useState(''),[domain,setDomain]=useState<Domain>('dataQualityIssues'),[q,setQ]=useState(''),[selected,setSelected]=useState<any>(null);useEffect(()=>{authenticatedFetch(`/api/admin-command?role=${encodeURIComponent(role)}`,{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.controls)}).catch(e=>setError(e.message))},[role]);const rows=useMemo(()=>((data?.[domain])||[]).filter((r:any)=>JSON.stringify(r).toLowerCase().includes(q.toLowerCase())),[data,domain,q]);if(!data)return <State text={error||'Loading canonical compliance controls…'}/>;const tabs:[Domain,string][]=[['dataQualityIssues','Data quality'],['duplicateCandidates','Duplicates'],['credentials','Credentials'],['backgroundChecks','Background checks'],['safeSport','Safe Sport'],['waivers','Waivers']];return <main className="p-5 text-white lg:p-7"><div className="text-[9px] font-black uppercase tracking-[.22em] text-[#FA4616]">Organization Administrator · Compliance</div><h1 className="mt-1 text-3xl font-black">Compliance control queue</h1><p className="mt-2 text-sm text-neutral-400">Live controls and exceptions from the authorized organization scope. Compliance is a function, not a role.</p><div className="mt-5 flex flex-wrap gap-2">{tabs.map(([k,n])=><button key={k} onClick={()=>setDomain(k)} className={domain===k?'rounded-lg bg-[#FA4616] px-3 py-2 text-xs font-black text-black':'rounded-lg border border-neutral-700 px-3 py-2 text-xs'}>{n} · {(data[k]||[]).length}</button>)}</div><section className="mt-4 overflow-hidden rounded-2xl border border-neutral-800 bg-[#090b0b]"><div className="border-b border-neutral-800 p-4"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter live control records" className="w-full max-w-sm rounded-lg border border-neutral-700 bg-black px-3 py-2 text-sm"/></div><div className="divide-y divide-neutral-800">{rows.map((r:any)=><button key={r.id} onClick={()=>setSelected(r)} className="grid w-full grid-cols-[1fr_auto] gap-4 p-4 text-left hover:bg-white/[.03]"><span><b>{r.name||r.credential_type||r.check_type||r.certification_type||r.entity_type||r.code||r.id}</b><span className="mt-1 block text-xs text-neutral-500">{r.severity||r.verification_status||r.result_classification||r.required_for||''}</span></span><span className="text-xs uppercase text-neutral-400">{r.status||'recorded'}</span></button>)}</div>{!rows.length&&<div className="p-8 text-sm text-neutral-500">No canonical records exist for this control domain.</div>}</section>{selected&&<div className="fixed inset-0 z-[100] bg-black/70" onClick={()=>setSelected(null)}><aside onClick={e=>e.stopPropagation()} className="ml-auto h-full w-full max-w-2xl overflow-y-auto border-l border-neutral-700 bg-[#080909] p-6"><button onClick={()=>setSelected(null)} className="float-right text-neutral-400">Close</button><div className="text-[9px] uppercase tracking-[.2em] text-[#FA4616]">Compliance record</div><div className="mt-6 grid gap-3 sm:grid-cols-2">{Object.entries(selected).map(([k,v])=><div key={k} className="border-b border-neutral-800 pb-2"><div className="text-[9px] uppercase text-neutral-600">{k.replaceAll('_',' ')}</div><div className="break-words text-sm">{typeof v==='object'?JSON.stringify(v):String(v??'—')}</div></div>)}</div></aside></div>}</main>}function State({text}:{text:string}){return <div className="p-8 text-white"><h1 className="text-2xl font-black">Compliance</h1><p className="mt-2 text-neutral-500">{text}</p></div>}export default Compliance;