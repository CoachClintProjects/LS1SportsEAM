'use client';import{useEffect,useState}from'react';import{authenticatedFetch}from'@/lib/client/authenticatedFetch';export function VendorDirectory(){const[rows,setRows]=useState<any[]>([]),[error,setError]=useState(''),[selected,setSelected]=useState<any>(null);useEffect(()=>{authenticatedFetch('/api/admin-command',{cache:'no-store'}).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setRows(j.enterprise?.vendors||[])}).catch(e=>setError(e.message))},[]);return <main className="p-5 text-white lg:p-7"><div className="text-[9px] font-black uppercase tracking-[.22em] text-[#FA4616]">Organization Administrator · Live records</div><h1 className="mt-1 text-3xl font-black">Vendors</h1>{error&&<p className="mt-4 text-red-300">{error}</p>}<section className="mt-5 overflow-hidden rounded-2xl border border-neutral-800 bg-[#090b0b]">{rows.map(r=><button key={r.id} onClick={()=>setSelected(r)} className="flex w-full justify-between border-b border-neutral-800 p-4 text-left hover:bg-white/[.03]"><b>{r.name||r.code||r.id}</b><span className="text-xs uppercase text-neutral-500">{r.status||''}</span></button>)}{!rows.length&&!error&&<div className="p-8 text-sm text-neutral-500">No canonical records exist in this domain.</div>}</section>{selected&&<div className="fixed inset-0 z-[100] bg-black/70" onClick={()=>setSelected(null)}><aside onClick={e=>e.stopPropagation()} className="ml-auto h-full w-full max-w-2xl overflow-y-auto border-l border-neutral-700 bg-[#080909] p-6"><button onClick={()=>setSelected(null)}>Close</button><pre className="mt-6 whitespace-pre-wrap text-xs">{JSON.stringify(selected,null,2)}</pre></aside></div>}</main>}export default VendorDirectory;