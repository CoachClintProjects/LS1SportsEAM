'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import { Activity, AlertTriangle, ChevronLeft, ChevronRight, Database, ExternalLink, RefreshCw, ShieldCheck, X } from 'lucide-react';
import { authenticatedFetch } from '@/lib/client/authenticatedFetch';

type Row=Record<string,unknown>;
type MetricRow={table:string;label:string;count:number|null};
type ModulePayload={view:string;label:string;metrics:MetricRow[];generatedAt:string;source:string;error?:string};
type CommandPayload={tasks?:Array<Record<string,unknown>>;generatedAt?:string;source?:string;error?:string};
type RecordPayload={table:string;rows:Row[];columns:string[];limit:number;offset:number;returned:number;generatedAt:string;source:string;error?:string};
type WorkspaceMeta={section:string;description:string};
const FINANCE=new Set(['financial-overview','general-ledger','receivables','payables','revenue','costs','budgets','forecasting','profitability','cash-flow']);
const SECURITY=new Set(['identity','roles','permissions','raci','delegation','sod','privileged-access']);
const GOVERNANCE=new Set(['compliance','data-governance','privacy','retention','audit']);
const TEAM_ENGINE=new Set(['team-engine','organizations','people','registrar','rosters','memberships','programs','teams','seasons']);
const OPERATIONS=new Set(['procurement','payroll','workflow','rules','integrations','imports','reporting','facilities','assets','maintenance','resources']);
const INTELLIGENCE=new Set(['agents','automation','alerts','insights','athletes']);
const ENGINEERING=new Set(['product','deployments','system-health','knowledge','platform','settings','role-customization']);
const CLIENTS=new Set(['all-clients','new-client','onboarding-queue','active-clients','client-exceptions','client-updates']);
function humanize(value:string){return value.split('-').filter(Boolean).map(part=>part.charAt(0).toUpperCase()+part.slice(1)).join(' ')}
function metaFor(view:string):WorkspaceMeta{if(TEAM_ENGINE.has(view))return{section:'TEAM ENGINE',description:'Canonical organizations, people, programs, seasons, teams, memberships, rosters and registrar evidence.'};if(FINANCE.has(view))return{section:'FINANCE & ACCOUNTING',description:'Live financial control records, evidence, and accounting operating data.'};if(SECURITY.has(view))return{section:'IDENTITY & SECURITY',description:'Identity, role, permission, authority, and privileged-access control records.'};if(GOVERNANCE.has(view))return{section:'GOVERNANCE',description:'Compliance, privacy, audit, lineage, retention, and data-governance evidence.'};if(OPERATIONS.has(view))return{section:'OPERATIONS',description:'Operational records and workflow evidence for this LS1Sports domain.'};if(INTELLIGENCE.has(view))return{section:'INTELLIGENCE',description:'Athlete, agent, automation, alert, and evidence-backed intelligence records.'};if(ENGINEERING.has(view))return{section:'ENGINEERING / PLATFORM',description:'Platform delivery, configuration, release, system-health, and operating evidence.'};if(CLIENTS.has(view))return{section:'CLIENT OPERATIONS',description:'Client lifecycle, onboarding, activation, exceptions, and update records.'};return{section:'ENTERPRISE',description:'Live canonical LS1Sports records for this enterprise domain.'}}
function Panel({children,className=''}:{children:React.ReactNode;className?:string}){return <section className={`rounded-2xl border border-neutral-800 bg-[#090b0b] p-5 lg:p-6 ${className}`}>{children}</section>}
function formatValue(value:unknown){if(value===null||value===undefined)return'—';if(typeof value==='object'){const text=JSON.stringify(value);return text.length>180?`${text.slice(0,177)}…`:text}const text=String(value);return text.length>180?`${text.slice(0,177)}…`:text}

export default function SuperUserModuleWorkspace({view}:{view:string}){
 const safeView=view?.trim()||'platform',requestView=safeView==='team-engine'?'team-manager':safeView,meta=metaFor(safeView);
 const[moduleData,setModuleData]=useState<ModulePayload|null>(null),[command,setCommand]=useState<CommandPayload|null>(null),[loading,setLoading]=useState(true),[refreshing,setRefreshing]=useState(false),[error,setError]=useState(''),[selectedMetric,setSelectedMetric]=useState<MetricRow|null>(null),[records,setRecords]=useState<RecordPayload|null>(null),[recordLoading,setRecordLoading]=useState(false),[recordError,setRecordError]=useState('');
 const load=useCallback(async(background=false)=>{background?setRefreshing(true):setLoading(true);setError('');try{const[moduleResponse,commandResponse]=await Promise.all([authenticatedFetch(`/api/superuser-module?view=${encodeURIComponent(requestView)}`,{cache:'no-store'}),authenticatedFetch('/api/superuser-command',{cache:'no-store'})]);const moduleJson=await moduleResponse.json().catch(()=>null),commandJson=await commandResponse.json().catch(()=>null);if(!moduleResponse.ok)throw new Error(moduleJson?.error||`Workspace API returned ${moduleResponse.status}`);setModuleData(moduleJson as ModulePayload);setCommand(commandResponse.ok?commandJson as CommandPayload:{tasks:[],error:commandJson?.error})}catch(cause){setError(cause instanceof Error?cause.message:'Unable to load this workspace.')}finally{setLoading(false);setRefreshing(false)}},[requestView]);
 const openRecords=useCallback(async(metric:MetricRow,offset=0)=>{setSelectedMetric(metric);setRecordLoading(true);setRecordError('');try{const response=await authenticatedFetch(`/api/superuser-records?table=${encodeURIComponent(metric.table)}&limit=25&offset=${offset}`,{cache:'no-store'}),json=await response.json().catch(()=>null);if(!response.ok)throw new Error(json?.error||`Record API returned ${response.status}`);setRecords(json as RecordPayload)}catch(cause){setRecords(null);setRecordError(cause instanceof Error?cause.message:'Unable to load records.')}finally{setRecordLoading(false)}},[]);
 useEffect(()=>{void load()},[load]);
 const relatedTasks=useMemo(()=>{const terms=[safeView,humanize(safeView).toLowerCase()];return(command?.tasks??[]).filter(task=>{const text=[task.code,task.name,task.description,task.status].filter(Boolean).join(' ').toLowerCase();return terms.some(term=>text.includes(term.toLowerCase()))}).slice(0,10)},[command?.tasks,safeView]);
 if(loading&&!moduleData)return <Panel><div className="flex min-h-48 items-center justify-center gap-3 text-neutral-500"><RefreshCw className="h-5 w-5 animate-spin"/><span className="text-sm">Loading live workspace…</span></div></Panel>;
 if(error&&!moduleData)return <Panel><div className="rounded-xl border border-red-500/30 bg-red-500/5 p-5"><div className="flex items-start gap-3"><AlertTriangle className="mt-0.5 h-5 w-5 shrink-0 text-red-400"/><div><h2 className="font-black text-white">Workspace could not load</h2><p className="mt-1 text-sm text-neutral-400">{error}</p><button onClick={()=>void load()} className="mt-4 rounded-lg bg-white px-3 py-2 text-xs font-black text-black">Retry</button></div></div></div></Panel>;
 const metrics=moduleData?.metrics??[];
 return <div className="space-y-6"><Panel><div className="flex flex-wrap items-start justify-between gap-5"><div className="max-w-4xl"><div className="text-[9px] font-black uppercase tracking-[.25em] text-emerald-400">{meta.section}</div><h1 className="mt-2 text-3xl font-black text-white">{safeView==='team-engine'?'Team Engine':moduleData?.label??humanize(safeView)}</h1><p className="mt-2 text-sm leading-6 text-neutral-400">{meta.description}</p></div><button onClick={()=>void load(true)} disabled={refreshing} className="inline-flex items-center gap-2 rounded-xl border border-neutral-700 px-4 py-3 text-xs font-black text-neutral-300 hover:border-emerald-500/50 hover:text-white disabled:opacity-50"><RefreshCw className={`h-4 w-4 ${refreshing?'animate-spin':''}`}/>Refresh live data</button></div></Panel>
 {error&&<div className="flex items-center gap-2 rounded-xl border border-amber-500/30 bg-amber-500/5 px-4 py-3 text-xs text-amber-200"><AlertTriangle className="h-4 w-4"/>{error}</div>}
 <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(metric=><button key={metric.table} onClick={()=>void openRecords(metric,0)} className="group rounded-xl border border-neutral-800 bg-[#0d1010] p-4 text-left transition hover:border-emerald-500/40 hover:bg-[#101414]"><div className="flex items-start justify-between gap-3"><div className="text-[9px] font-black uppercase tracking-[.18em] text-neutral-500">{metric.label}</div><ExternalLink className="h-3.5 w-3.5 text-neutral-700 group-hover:text-emerald-400"/></div><div className="mt-3 text-3xl font-black text-white">{metric.count??'—'}</div><div className="mt-1 font-mono text-[10px] text-neutral-500">{metric.table}</div><div className="mt-3 text-[10px] font-bold text-emerald-400">Open live records →</div></button>)}</div>
 <div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><Panel><div className="flex items-center gap-2"><Activity className="h-4 w-4 text-emerald-400"/><div><div className="text-[9px] font-black uppercase tracking-[.18em] text-neutral-500">Implementation state</div><h2 className="text-xl font-black text-white">Related build evidence</h2></div></div><div className="mt-5 space-y-2">{relatedTasks.length?relatedTasks.map((task,index)=><div key={String(task.id??task.code??index)} className="grid gap-3 rounded-xl border border-neutral-800 bg-[#0d1010] p-4 sm:grid-cols-[1fr_auto] sm:items-center"><div><div className="text-sm font-bold text-white">{String(task.code??'TASK')} · {String(task.name??'Unnamed task')}</div><div className="mt-1 text-[10px] text-neutral-500">{String(task.status??'UNKNOWN')}{task.blocker?` · blocker: ${String(task.blocker)}`:''}</div></div><div className="text-xl font-black text-white">{Number(task.percent_complete??0)}%</div></div>):<div className="rounded-xl border border-neutral-800 bg-[#0d1010] p-5 text-sm text-amber-300">No tracked implementation task is mapped to this domain. This remains a Super User action-plan gap.</div>}</div></Panel>
 <Panel><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-emerald-400"/><div><div className="text-[9px] font-black uppercase tracking-[.18em] text-neutral-500">Control posture</div><h2 className="text-xl font-black text-white">Live evidence</h2></div></div><div className="mt-5 space-y-3 text-xs"><div className="rounded-xl border border-neutral-800 bg-[#0d1010] p-4 text-neutral-400"><span className="font-bold text-white">Source:</span> {moduleData?.source??'LS1SportsEAM'}</div><div className="rounded-xl border border-neutral-800 bg-[#0d1010] p-4 text-neutral-400"><span className="font-bold text-white">Last refresh:</span> {moduleData?.generatedAt?new Date(moduleData.generatedAt).toLocaleString():'—'}</div><div className="rounded-xl border border-neutral-800 bg-[#0d1010] p-4 text-neutral-400"><span className="font-bold text-white">Records represented:</span> {metrics.reduce((sum,metric)=>sum+(metric.count??0),0)}</div><div className="rounded-xl border border-emerald-900/50 bg-emerald-950/10 p-4 text-emerald-300">Every metric drills into the live canonical records returned by the authenticated Super User API.</div></div></Panel></div>
 {selectedMetric&&<div className="fixed inset-0 z-[100] flex items-end justify-end bg-black/70 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label={selectedMetric.label} onMouseDown={()=>setSelectedMetric(null)}><div className="flex max-h-[90vh] w-full max-w-5xl flex-col rounded-2xl border border-neutral-700 bg-[#0a0d0d] shadow-2xl" onMouseDown={event=>event.stopPropagation()}><div className="flex items-start justify-between gap-4 border-b border-neutral-800 p-6"><div><div className="text-[9px] font-black uppercase tracking-[.2em] text-emerald-400">Live canonical records</div><h2 className="mt-2 text-2xl font-black text-white">{selectedMetric.label}</h2><div className="mt-1 flex items-center gap-2 font-mono text-[11px] text-neutral-500"><Database className="h-4 w-4 text-emerald-400"/>{selectedMetric.table}</div></div><button onClick={()=>setSelectedMetric(null)} className="rounded-lg border border-neutral-700 p-2 text-neutral-400 hover:text-white"><X className="h-4 w-4"/></button></div><div className="min-h-0 flex-1 overflow-auto p-6">{recordLoading?<div className="flex min-h-48 items-center justify-center gap-2 text-sm text-neutral-500"><RefreshCw className="h-4 w-4 animate-spin"/>Loading live records…</div>:recordError?<div className="rounded-xl border border-red-500/30 bg-red-500/5 p-4 text-sm text-red-200">{recordError}</div>:records?.rows.length?<div className="overflow-x-auto rounded-xl border border-neutral-800"><table className="min-w-full text-left text-xs"><thead className="sticky top-0 bg-[#111515] text-[9px] uppercase tracking-[.12em] text-neutral-500"><tr>{records.columns.slice(0,10).map(column=><th key={column} className="whitespace-nowrap border-b border-neutral-800 px-3 py-3">{humanize(column)}</th>)}</tr></thead><tbody>{records.rows.map((row,index)=><tr key={String(row.id??index)} className="border-b border-neutral-900 align-top last:border-b-0">{records.columns.slice(0,10).map(column=><td key={column} className="max-w-[260px] px-3 py-3 font-mono text-[10px] leading-4 text-neutral-300"><div className="max-h-20 overflow-auto break-words">{formatValue(row[column])}</div></td>)}</tr>)}</tbody></table></div>:<div className="rounded-xl border border-dashed border-neutral-800 p-8 text-center text-sm text-neutral-500">No records currently exist in this canonical table.</div>}</div><div className="flex items-center justify-between gap-3 border-t border-neutral-800 p-4"><div className="text-[10px] text-neutral-500">Showing {records?.returned??0} · offset {records?.offset??0} · live count {selectedMetric.count??'—'}</div><div className="flex gap-2"><button disabled={recordLoading||!records||records.offset<=0} onClick={()=>records&&void openRecords(selectedMetric,Math.max(0,records.offset-records.limit))} className="rounded-lg border border-neutral-700 p-2 text-neutral-300 disabled:opacity-30"><ChevronLeft className="h-4 w-4"/></button><button disabled={recordLoading||!records||records.returned<records.limit} onClick={()=>records&&void openRecords(selectedMetric,records.offset+records.limit)} className="rounded-lg border border-neutral-700 p-2 text-neutral-300 disabled:opacity-30"><ChevronRight className="h-4 w-4"/></button></div></div></div></div>}
 </div>
}
